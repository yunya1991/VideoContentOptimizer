[pytest]
testpaths = test
asyncio_mode = auto

markers =
    stress: 压力测试（并发 / 大文本 / 超时），本地验证用，CI 默认跳过

addopts =
    -v
    --tb=short
    -p no:warnings
    --strict-markers

# CI 默认排除 stress 测试（耗时较长）
# 本地全量：pytest
# 本地仅压力：pytest -m stress
# CI 推荐：pytest -m "not stress"
